import integer_linear_problem as ILP


for hubs: Set in Powerset(N):
    
    for hub in subset:
        hub.isHub = True
    
    for non-hub in N:
        connect with hub such that cost is minimal
    
    min_graph = current_graph if z() < z_min